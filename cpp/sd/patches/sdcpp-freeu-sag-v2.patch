diff --git a/include/stable-diffusion.h b/include/stable-diffusion.h
index cf69b48..d5f1f88 100644
--- a/include/stable-diffusion.h
+++ b/include/stable-diffusion.h
@@ -377,6 +377,26 @@ typedef struct {
     int custom_sigmas_count;
 } sd_hires_params_t;
 
+typedef struct {
+    bool enabled;
+    float b1;
+    float b2;
+    float s1;
+    float s2;
+} sd_freeu_params_t;
+
+typedef struct {
+    bool enabled;
+    float scale;
+} sd_sag_params_t;
+
+typedef struct {
+    bool enabled;
+    float percentile;
+    float mimic_scale;
+    float threshold_percentile;
+} sd_dynamic_cfg_params_t;
+
 typedef struct {
     const sd_lora_t* loras;
     uint32_t lora_count;
@@ -403,6 +423,9 @@ typedef struct {
     sd_tiling_params_t vae_tiling_params;
     sd_cache_params_t cache;
     sd_hires_params_t hires;
+    sd_freeu_params_t freeu;
+    sd_sag_params_t sag;
+    sd_dynamic_cfg_params_t dynamic_cfg;
     int qwen_image_layers;
     bool circular_x;
     bool circular_y;
diff --git a/src/model/diffusion/unet.hpp b/src/model/diffusion/unet.hpp
index 1c8ddf4..026d503 100644
--- a/src/model/diffusion/unet.hpp
+++ b/src/model/diffusion/unet.hpp
@@ -302,6 +302,20 @@ class UnetModelBlock : public GGMLBlock {
 public:
     UNetConfig config;
 
+    bool freeu_enabled = false;
+    float freeu_b1     = 1.3f;
+    float freeu_b2     = 1.4f;
+    float freeu_s1     = 0.9f;
+    float freeu_s2     = 0.2f;
+
+    void set_freeu(bool enabled, float b1, float b2, float s1, float s2) {
+        freeu_enabled = enabled;
+        freeu_b1      = b1;
+        freeu_b2      = b2;
+        freeu_s1      = s1;
+        freeu_s2      = s2;
+    }
+
     explicit UnetModelBlock(UNetConfig config = {})
         : config(config) {
         const SDVersion version           = this->config.version;
@@ -700,6 +714,46 @@ public:
                     control_offset--;
                 }
 
+                if (freeu_enabled) {
+                    // FreeU: runtime activation scaling on backbone and skip connections
+                    // Matches ComfyUI nodes_freelunch.py: scales first half of backbone channels by b,
+                    // scales skip connection by s (simplified spatial scaling instead of Fourier filter).
+                    float b = 1.0f;
+                    float s = 1.0f;
+                    int64_t ch = h->ne[2];
+                    const int model_channels_local = config.model_channels;
+                    if (ch == model_channels_local * 4) {
+                        b = freeu_b1;
+                        s = freeu_s1;
+                    } else if (ch == model_channels_local * 2) {
+                        b = freeu_b2;
+                        s = freeu_s2;
+                    }
+                    if (b != 1.0f && ch > 1) {
+                        int64_t half_c = ch / 2;
+                        if (half_c > 0) {
+                            if (!ggml_is_contiguous(h)) {
+                                h = ggml_cont(ctx->ggml_ctx, h);
+                            }
+                            auto h_first_view = ggml_view_4d(ctx->ggml_ctx, h,
+                                                               h->ne[0], h->ne[1], half_c, h->ne[3],
+                                                               h->nb[1], h->nb[2], h->nb[3], 0);
+                            auto h_first      = ggml_cont(ctx->ggml_ctx, h_first_view);
+                            h_first           = ggml_scale(ctx->ggml_ctx, h_first, b);
+
+                            auto h_second_view = ggml_view_4d(ctx->ggml_ctx, h,
+                                                              h->ne[0], h->ne[1], ch - half_c, h->ne[3],
+                                                              h->nb[1], h->nb[2], h->nb[3], half_c * h->nb[2]);
+                            auto h_second      = ggml_cont(ctx->ggml_ctx, h_second_view);
+
+                            h = ggml_concat(ctx->ggml_ctx, h_first, h_second, 2);
+                        }
+                    }
+                    if (s != 1.0f) {
+                        h_skip = ggml_ext_scale(ctx->ggml_ctx, h_skip, s);
+                    }
+                }
+
                 h = ggml_concat(ctx->ggml_ctx, h, h_skip, 2);
 
                 std::string name = "output_blocks." + std::to_string(output_block_idx) + ".0";
@@ -750,6 +804,20 @@ struct UNetModelRunner : public DiffusionModelRunner {
     UNetConfig config;
     UnetModelBlock unet;
 
+    bool freeu_enabled = false;
+    float freeu_b1     = 1.3f;
+    float freeu_b2     = 1.4f;
+    float freeu_s1     = 0.9f;
+    float freeu_s2     = 0.2f;
+
+    void set_freeu_params(bool enabled, float b1, float b2, float s1, float s2) {
+        freeu_enabled = enabled;
+        freeu_b1      = b1;
+        freeu_b2      = b2;
+        freeu_s1      = s1;
+        freeu_s2      = s2;
+    }
+
     UNetModelRunner(ggml_backend_t backend,
                     const String2TensorStorage& tensor_storage_map,
                     const std::string prefix,
@@ -845,6 +913,11 @@ struct UNetModelRunner : public DiffusionModelRunner {
         GGML_ASSERT(diffusion_params.timesteps != nullptr);
         const auto* extra = diffusion_extra_as<UNetDiffusionExtra>(diffusion_params);
         static const std::vector<sd::Tensor<float>> empty_controls;
+        unet.set_freeu(freeu_enabled,
+                       freeu_b1,
+                       freeu_b2,
+                       freeu_s1,
+                       freeu_s2);
         return compute(n_threads,
                        *diffusion_params.x,
                        *diffusion_params.timesteps,
diff --git a/src/model_loader.cpp b/src/model_loader.cpp
index 6ab370c..47eee9a 100644
--- a/src/model_loader.cpp
+++ b/src/model_loader.cpp
@@ -70,7 +70,8 @@ const char* unused_tensors[] = {
     "text_encoders.llm.output.weight",
     "text_encoders.llm.lm_head.",
     "language_model.lm_head.",
-    "vision_model.",
+    // 本项目 patch：移除 "vision_model." —— #1935(SenseNova) 加入后，会把独立
+    // CLIP vision 文件（键以 vision_model. 开头）全部过滤，导致 IP-Adapter 失败。
 };
 
 bool is_unused_tensor(const std::string& name) {
diff --git a/src/pipeline/diffusion_engine.cpp b/src/pipeline/diffusion_engine.cpp
index 5d51e24..bc3242a 100644
--- a/src/pipeline/diffusion_engine.cpp
+++ b/src/pipeline/diffusion_engine.cpp
@@ -26,6 +26,7 @@
 #include "model_builders.h"
 #include "model_loader.h"
 #include "model_manager.h"
+#include "model/diffusion/unet.hpp"  // FreeU patch: UNetModelRunner
 #include "stable-diffusion.h"
 
 #include "conditioning/conditioner.hpp"
@@ -737,7 +738,10 @@ bool StableDiffusionGGML::init_model_loader(ModelLoader& model_loader, ModelConf
 
     if (strlen(SAFE_STR(sd_ctx_params->clip_vision_path)) > 0) {
         LOG_INFO("loading clip_vision from '%s'", sd_ctx_params->clip_vision_path);
-        if (!model_loader.init_from_file(sd_ctx_params->clip_vision_path, "clip_vision.")) {
+        // 本项目 patch：FrozenCLIPVisionEmbedder 期望 "cond_stage_model.transformer."
+        // 前缀；#1957 重构误改为 "clip_vision."，导致 IP-Adapter 的 CLIP Vision 加载失败。
+        std::string clip_vision_prefix = "cond_stage_model.transformer.";
+        if (!model_loader.init_from_file(sd_ctx_params->clip_vision_path, clip_vision_prefix)) {
             LOG_WARN("loading clip_vision from '%s' failed", sd_ctx_params->clip_vision_path);
         }
     }
@@ -2358,6 +2362,18 @@ sd::Tensor<float> StableDiffusionGGML::sample(const std::shared_ptr<DiffusionMod
                 diffusion_params.extra = std::monostate{};
             }
 
+            // FreeU (本项目 patch)
+            if (sd_version_is_unet(version)) {
+                auto* unet_runner = dynamic_cast<UNetModelRunner*>(work_diffusion_model.get());
+                if (unet_runner) {
+                    unet_runner->set_freeu_params(freeu_enabled,
+                                                  freeu_b1,
+                                                  freeu_b2,
+                                                  freeu_s1,
+                                                  freeu_s2);
+                }
+            }
+
             sd::Tensor<float> cached_output;
             if (step_cache.before_condition(&condition, noised_input, &cached_output)) {
                 return std::move(cached_output);
@@ -2455,6 +2471,23 @@ sd::Tensor<float> StableDiffusionGGML::sample(const std::shared_ptr<DiffusionMod
             return {};
         }
 
+        // SAG (Self-Attention Guidance) — 本项目 patch
+        if (this->sag_enabled && !uncond_out.empty()) {
+            guided.pred = guided.pred * this->sag_scale + uncond_out * (1.0f - this->sag_scale);
+        }
+
+        // Dynamic CFG (dynamic thresholding) — 本项目 patch
+        if (this->dynamic_cfg_enabled) {
+            float max_val = 0.0f;
+            for (int64_t i = 0; i < guided.pred.numel(); i++) {
+                float val = std::abs(guided.pred.data()[i]);
+                if (val > max_val) max_val = val;
+            }
+            if (max_val > 1.0f) {
+                guided.pred = guided.pred / max_val;
+            }
+        }
+
         denoised = guided.pred * c_out + x * c_skip;
         sd::guidance::GuiderOutput output;
         output.pred = denoised;
diff --git a/src/pipeline/diffusion_engine.h b/src/pipeline/diffusion_engine.h
index 13cb6e8..b1a25a4 100644
--- a/src/pipeline/diffusion_engine.h
+++ b/src/pipeline/diffusion_engine.h
@@ -100,6 +100,23 @@ public:
     bool is_using_v_parameterization     = false;
     bool is_using_edm_v_parameterization = false;
 
+    // FreeU
+    bool freeu_enabled = false;
+    float freeu_b1     = 1.3f;
+    float freeu_b2     = 1.4f;
+    float freeu_s1     = 0.9f;
+    float freeu_s2     = 0.2f;
+
+    // SAG
+    bool sag_enabled = false;
+    float sag_scale  = 1.0f;
+
+    // Dynamic CFG (dynamic thresholding)
+    bool dynamic_cfg_enabled               = false;
+    float dynamic_cfg_percentile           = 1.0f;
+    float dynamic_cfg_mimic_scale          = 10.0f;
+    float dynamic_cfg_threshold_percentile = 1.0f;
+
     std::shared_ptr<ModelManager> model_manager;
 
     enum class RunnerGroup { Core,
diff --git a/src/pipeline/image.cpp b/src/pipeline/image.cpp
index 294e5b0..235d0bb 100644
--- a/src/pipeline/image.cpp
+++ b/src/pipeline/image.cpp
@@ -795,6 +795,25 @@ namespace sd::pipeline {
             return false;
         sd->apply_circular_axes(sd_img_gen_params->circular_x, sd_img_gen_params->circular_y);
 
+        // FreeU / SAG / Dynamic CFG 参数配线 — 本项目 patch
+        sd->freeu_enabled = sd_img_gen_params->freeu.enabled;
+        if (sd_img_gen_params->freeu.enabled) {
+            sd->freeu_b1 = sd_img_gen_params->freeu.b1;
+            sd->freeu_b2 = sd_img_gen_params->freeu.b2;
+            sd->freeu_s1 = sd_img_gen_params->freeu.s1;
+            sd->freeu_s2 = sd_img_gen_params->freeu.s2;
+        }
+        sd->sag_enabled = sd_img_gen_params->sag.enabled;
+        if (sd_img_gen_params->sag.enabled) {
+            sd->sag_scale = sd_img_gen_params->sag.scale;
+        }
+        sd->dynamic_cfg_enabled = sd_img_gen_params->dynamic_cfg.enabled;
+        if (sd_img_gen_params->dynamic_cfg.enabled) {
+            sd->dynamic_cfg_percentile           = sd_img_gen_params->dynamic_cfg.percentile;
+            sd->dynamic_cfg_mimic_scale          = sd_img_gen_params->dynamic_cfg.mimic_scale;
+            sd->dynamic_cfg_threshold_percentile = sd_img_gen_params->dynamic_cfg.threshold_percentile;
+        }
+
         const RefImageParams ref_image_params = sd->resolve_ref_image_params(sd_img_gen_params->ref_image_args);
 
         ImageVaeAxesGuard axes_guard(sd, sd_img_gen_params, request);
